import 'package:flutter/cupertino.dart';

class Ministry_Page extends StatelessWidget {
  const Ministry_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text('Hello 0 ')],
    );
  }
}
